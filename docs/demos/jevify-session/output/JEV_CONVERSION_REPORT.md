# Jev conversion report

Converted only the approved pure DECISION site, original workflow.py:83, on jev-convert/demo. Original revision: 7c497d21099fc916692f1dd45b5a6107b03754d5 (main). No push.

The public simple_classify(X) signature and string result remain. The original Anthropic request, prompt, model claude-haiku-4-5, parsing and exceptions are preserved in _original_simple_classify. jev_decisions.py supplies one Choice over the original ten labels, pinned jev-1.13.0, provisional confidence threshold 0.95, input bound and bounded caller timeout. Original fallback remains on every rejected/error path and executes exactly once; original exceptions propagate. Path/model/reason are logged through Python logging without input or credentials; configure INFO logging to observe successful/fallback paths. No generated content or other sites changed.

Provider selection is none. JEV_PROVIDER=none is the default and bypasses Jev entirely; the optional direct TypeSafe adapter requires explicit future JEV_PROVIDER=typesafe plus TYPESAFE_API_KEY. .env.example contains placeholders only. Original operation still requires its Anthropic SDK and key; offline smoke substitutes only that SDK. No packages installed or providers contacted. The deadline bounds the caller through body reading/parsing; a daemon worker can briefly outlive a timed-out caller, with a socket timeout and no retries.

Validation: supplied offline project smoke PASSED; retained tests/offline_validation.py PASSED. Exact commands/output are in JEV_VALIDATION.txt. Source AST (allowing rename), original request equality, output stripping, stdout, fallback count and original exception identity were checked. Fault checks passed for low-confidence gate rejection, generic transport error, timeout, HTTP 429, missing key, invalid schema/confidence and oversized input. The gate test uses sentinels at the evaluator/gate boundary, not an invented HTTP-200 response; the threshold predicate is tested separately. Accepted-path response replay was skipped because no genuine capture exists.

**live validation skipped** — no keys, as explicitly requested. Live n=0; agreement, confidence curve, observed fallback rate, latency and cost are unknown, not zero. No parity or measured savings claim. JEV_PARITY.json records empty rows and executed offline faults. Tests used synthetic input and original-SDK doubles, not live inference. Available public fixture provenance: data/train.tsv and data/test.tsv, each 68 unique labeled tickets, from the Anthropic cookbook revision recorded in PROVENANCE.json.

Threshold recommendation for workflow.py:83: retain provisional 0.95 and conservative fallback; keep Jev disabled until representative paired validation can calibrate and confirm the gate. Future evaluation needs at least 30 unique original/Jev pairs, independent calibration/confirmation, raw and selective agreement, fallback fraction, p50/p95 latency, and measured or explicitly estimated cost. Likely savings arise only when Jev avoids original inference; misses add serial cost/latency. Fit remains limited to benign bundled examples. No effective adversarial-input mitigation or production robustness has been established, and confidence is not an injection defense.

Artifacts: JEV_CONVERSION_PLAN.md, JEV_AUDIT.json, JEV_PARITY.json, JEV_VALIDATION.txt and this report. Artifact shape check: passed (no --require-live because no key). Shape validation does not establish model quality.

Undo after ensuring no uncommitted work: `git switch main` then `git branch -D jev-convert/demo`. Nothing was pushed.
