{
  "headless": true,
  "output": {
    "mode": "clone",
    "clone_path": "../example-jev"
  },
  "access": {
    "provider": "openrouter",
    "key_env": "OPENROUTER_API_KEY",
    "model": "typesafe/jev-1.13-20260917"
  },
  "risk": "conservative",
  "approval": {
    "actions": [
      "fallback"
    ],
    "scope": "Only pure DECISION sites with enumerated outputs; leave all others unchanged."
  }
}
