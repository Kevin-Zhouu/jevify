# Existing Anthropic fallback credential
ANTHROPIC_API_KEY=
# Optional future TypeSafe direct access; no access configured for this run
TYPESAFE_API_KEY=
# Set to 1 only for reviewed, trusted offline input use
JEV_TRUSTED_INPUTS=0
