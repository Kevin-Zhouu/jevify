diff --git a/corpus/vercel_enum/workflow.ts b/eval/results/dev-03/vercel_enum-codex-headless/original/workflow.ts
index 61508ea..cf6d881 100644
--- a/corpus/vercel_enum/workflow.ts
+++ b/eval/results/dev-03/vercel_enum-codex-headless/original/workflow.ts
@@ -1,9 +1,12 @@
 import { openai } from '@ai-sdk/openai';
 import { generateObject } from 'ai';
 import 'dotenv/config';
+import { withGenreDecision } from './jevDecisions';
 
 async function main() {
-  const result = await generateObject({
+  const plot = 'A group of astronauts travel through a wormhole in search of a ' +
+    'new habitable planet for humanity.';
+  const result = await withGenreDecision(plot, () => generateObject({
     model: openai('gpt-4o-mini', { structuredOutputs: true }),
     output: 'enum',
     enum: ['action', 'comedy', 'drama', 'horror', 'sci-fi'],
@@ -11,7 +14,7 @@ async function main() {
       'Classify the genre of this movie plot: ' +
       '"A group of astronauts travel through a wormhole in search of a ' +
       'new habitable planet for humanity."',
-  });
+  }));
 
   console.log(result.object);
   console.log();
