# Landing page evidence

The call-site diff is mechanically generated from the committed `corpus/vercel_enum/workflow.ts` and `eval/results/dev-03/vercel_enum-codex-headless/original/workflow.ts` in Kevin-Zhouu/jevify at commit c3cdda6. The policy excerpts in app/evidence.json are exact extracts of that run’s jevDecisions.ts. This is existing DEV03 evidence, not a new run. The report and raw parity file are linked on the page.

The separate 0.44s / 5.11s comparison is the original synthetic JSON demo; it is not a recording of Jevify editing a repository. Its assets and measurements remain unchanged.

Minecraft links to its creator’s original post and code; third-party video URLs are no longer embedded.
