# Jevify landing page

Responsive product page with the recorded Claude-versus-Jev demo.

Run `npm ci`, then `npm run dev`. Production build: `npm run build`.

No model API keys or runtime secrets are used. The demo is a static capture.
